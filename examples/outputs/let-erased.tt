-- vim: ft=ttstar
  let
    postulate Z : Nat
    postulate S : (x) -> Nat
    plus 
       Z m = m
       (S n) m = S (plus n m)
    const = (\x. (\y. x))
    main = 
      let
        apply 
           f Z = f Z
           f (S x) = f (S x)
        postulate Q : Nat
        three = 3
      in plus (apply (\x. x) 2) (apply (const Q) three)
  in main
