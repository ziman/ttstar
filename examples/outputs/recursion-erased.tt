-- vim: ft=ttstar
  let
    postulate Z : Nat
    postulate S : (n) -> Nat
    postulate VNil : Vec
    postulate VCons : (xs) -> Vec
    vlen 
       VNil = Z
       (VCons xs) = S (vlen xs)
    testVec = VCons (VCons VNil)
    main = vlen testVec
  in main
