-- vim: ft=ttstar

  let
    constructor Just : (x) -> Maybe
    constructor Nothing : Maybe
    constructor Bool : Type
    constructor False : Bool
    f 
      [_] (Just b) = b
      [_] Nothing = Bool
    main = f (Just False)
  in main
