-- vim: ft=idris

data N : Type.
  Z : N,
  S : N -> N.

plus : N -> N -> N.
plus (m : N) (n : n) =
  case m
  | Z    -> n
  | S m' -> S (plus m' n)
  .

main :R: N
  = let
      id : N -> N.
      id (x : N) = x.
    in plus (id 3) (id 4).
