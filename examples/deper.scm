(require-extension matchable)
(print
  (letrec* (
    (T `(T))
    (F `(F))
    (TB (lambda (x)
      (lambda (y)
        `(TB ,x ,y))))
    (id (lambda (x)
      x))
    (constT T)
    (f (lambda (_e0)
      (match (list _e0)
        [(('T))
          id]
        [(('F))
          constT])))
    (main ((TB ((f T) F)) (f F)))
  )
    main))
